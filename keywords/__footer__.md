The specification ends here.

Before you consider the work done, verify all of the following:

1. Every declared structure, function, flow, and surface above is implemented — walk the blocks one by one and confirm each is satisfied.
2. No prohibited pattern appears anywhere in your output, including code adapted from examples.
3. Names, types, field sets, and signatures match their declarations exactly — no renames, no extra fields, no approximations.
4. The project builds and the declared test criteria pass; if you cannot run them, say so explicitly rather than implying success.

Then summarize what you implemented, mapping each major piece of code back to the specification block it satisfies, and list anything you could not complete with the reason why.
